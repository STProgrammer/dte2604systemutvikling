{% extends "layout.twig" %}
{% block content %}


<br>
    <br><br>
    <br>
    <br>
    <br>
    <br><br><br>
    <br>




    <section class="edit_group" id="edit_group">
        <div class="openContainerNoBorder text" id="table_register">
            <h3>Lag ny gruppe</h3>
            <div class>
                {% if session.getFlashBag.has('header') %}
                    {% for header in session.getFlashBag.get('header', []) %}
                        <h3>{{ header }}</h3>
                    {% endfor %}
                    {% for message in session.getFlashBag.get('message', []) %}
                        <p>{{ message }}</p>
                    {% endfor %}
                {% endif %}
            </div>
            <div id="text">
                <br>Gruppenavn: {{ group.getGroupName }}
                <br>Gruppeleder: {{ group.getFirstName }} {{ group.getLastName }}
                {% if group.isAdmin %}
                <br>Gruppe type: admin gruppe
                {% else %}
                <br>Gruppe type: vanlig gruppe
                {% endif %}


                <p><form method="post" id="group_register_form">
                    <br><label>Gruppenavn: </label>
                    <br><input type="text" name="groupName" value="" placeholder="Gruppenavn" maxlength=45 required
                               id="groupName"/>
                    <br><label>Velg gruppeleder:</label>
                    <br><div class="wrapper">
                        <select name="groupLeader" id="" class="form-control" onfocus='this.size=10;' onblur='this.size=10;' onchange='this.size=10; this.blur();'>
                            {% for empl in employees %}
                            <option value="{{ empl.getUserID() }}">{{ empl.getFirstName }} {{ empl.getLastName }} (ID: {{ empl.getUserId }})</option>
                            {% endfor %}
                        </select></div>
                    <br><label>Legg til brukere på gruppen: </label>
                    <br><div class="wrapper">
                        <select name="groupMembers[]" id="" class="form-control" multiple onfocus='this.size=10;' onblur='this.size=10;' onchange='this.size=10; this.blur();'>
                            {% for empl in employees %}
                                <option value="{{ empl.getUserID() }}">{{ empl.getFirstName }} {{ empl.getLastName }} (ID: {{ empl.getUserId }})</option>
                            {% endfor %}
                        </select></div>
                    {% if user.isAdmin %}
                    <br><label>Admin eller vanlig gruppe: </label>
                    <br><input type="radio" name="isAdmin" value="1" required>
                    <label>Admin</label><br>
                    <input type="radio" name="isAdmin" value="0" required>
                    <label>Vanlig</label><br>
                    {% endif %}
                    <input type="hidden" value="{{ getMac("Group edit") }}" name="XSRFPreventionToken">
                    <br><input type="submit" name="group_edit" id="reg_btn" value="Endre gruppe">
                </form></p>
                {% if user.isAdmin() %}
                    <form method="post"
                          onsubmit="return confirm('Er du sikker på å slette gruppen? \n(Kan ikke angres etterpå)');">
                        <input type="hidden" value="{{ getMac("Delete group") }}" name="XSRFPreventionToken">
                        <br><input type="submit" name="group_delete" value="Slett gruppe" class="delete"/>
                    </form>
                {% endif %}
            </div>
            <button class="button miniButtons" onclick="goBack()">Gå tilbake</button>

{% endblock %}